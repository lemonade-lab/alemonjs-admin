{
  "name": "alemonjs",
  "version": "0.0.1",
  "description": "",
  "author": "ningmengchongshui",
  "type": "module",
  "scripts": {
    "app": "yarn --cwd packages/admin dev",
    "dev": "yarn --cwd packages/web dev",
    "build": "npx lerna run build",
    "format": "prettier --write .",
    "check-format": "git diff --exit-code",
    "prepare": "husky"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.9",
    "husky": "^9.0.11"
  }
}
